# --- Core orchestration ----------------------------------------------------
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0

# --- LLM / embedding providers (optional, activated via EMBEDDING_PROVIDER) -
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.2.0
langchain-huggingface>=0.1.0
openai>=1.40.0

# --- Vector stores ---------------------------------------------------------
langchain-chroma>=0.1.4
chromadb>=0.5.0
langchain-pinecone>=0.2.0
pinecone>=5.0.0           # replaces the deprecated pinecone-client

# --- Document loaders ------------------------------------------------------
pypdf>=4.3.0
docx2txt>=0.8
unstructured>=0.15.0

# --- API service -----------------------------------------------------------
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.8.0
pydantic-settings>=2.5.0

# --- Retrieval extras (stretch goal: reranking, semantic chunking) ---------
sentence-transformers>=3.0.0
langchain-experimental>=0.3.0

# --- Evaluation ------------------------------------------------------------
ragas>=0.1.0

# --- Observability ---------------------------------------------------------
structlog>=24.0.0
prometheus-client>=0.20.0
tenacity>=9.0.0
python-dotenv>=1.0.0

# --- Testing / tooling -----------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0
pyflakes>=3.2.0
